    Tabs for Local/SD storage switching

    Coloring games in Local storage that are found on SD card in the game list

    Copy to SD single and multiple games at a time

    Option to move the XCI information to a separate window, possibly docking to the main window on the side (it's not necessary to always be visible)

    Better display for games, maybe pull the icon out of each game and cache it to a file and have multiple display modes: List, List w/ details (size, cart title, trimmed, etc), and Icon-only mode.

    Option to filter/search games

    Option to mark games that have empty certs

    Fix displaying root-directory XCIs

    Batch trimming of XCIs

    Batch renaming of XCIs to cart official name

    Download keys.txt from a config-loaded URL without relying on bat/powershell
